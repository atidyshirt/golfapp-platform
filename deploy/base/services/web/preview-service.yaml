apiVersion: v1
kind: Service
metadata:
  name: golfapp-web-preview
  labels:
    app: golfapp-web
spec:
  selector:
    app: golfapp-web
  ports:
    - name: http
      port: 80
      targetPort: http
